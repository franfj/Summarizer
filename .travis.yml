language: python

python:
  - "2.7" 
  
sudo: false

before_install:
- chmod +x summarizer/test/*.py

install:
  - pip install -r summarizer/requirements.txt
  
script:
  - summarizer/test/test.py
  
