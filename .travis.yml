sudo: false

language: python

python:
  - "2.7" 

before_install:
  - chmod +x summarizer/test/*.py

install:
  - pip install -r summarizer/requirements.txt
  
script:
  - python summarizer/test/test.py
  
